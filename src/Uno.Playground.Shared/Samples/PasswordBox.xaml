<UserControl x:Class="Uno.UI.Demo.Samples.PasswordBox"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:local="using:Uno.UI.Demo.Samples"
			 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
			 mc:Ignorable="">

	<ScrollViewer>
		<StackPanel Style="{StaticResource SamplePanelStyle}">

			<TextBlock Style="{StaticResource SampleSectionTextBlockStyle}"
					   Text="Normal"/>
			<PasswordBox Password="Password"
						 x:Name="PasswordBoxEnabled"/>

			<TextBlock Style="{StaticResource SampleSectionTextBlockStyle}"
					   Text="Disabled" />
			<PasswordBox Password="Password"
						 x:Name="PasswordBoxDisabled"
						 IsEnabled="False" />

			<TextBlock Style="{StaticResource SampleSectionTextBlockStyle}"
					   Text="Animated" />
			<PasswordBox Header="Animated"
						 x:Name="PasswordBoxAnimated"
						 Style="{StaticResource AnimatedPasswordBoxStyle}" />

		</StackPanel>
	</ScrollViewer>
</UserControl>
